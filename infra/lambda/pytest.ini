[pytest]
# Rooted here so the shared conftest.py is discovered for every Lambda below it.
# pythonpath puts this directory on sys.path so `from conftest import ...` works
# from a test file one level down.
pythonpath = .
testpaths = waitlist-signup waitlist-email waitlist-bounce waitlist-auth waitlist-actions waitlist-waves waitlist-gate waitlist-firstwin pipeline-filter webhook-receiver
