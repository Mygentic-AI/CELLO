{
  "//": {
    "overrides.@types/node": "Pinned because regenerating the lockfile re-resolves @types/node's floating range, and two copies in the tree produce 'IncomingMessage is not assignable to IncomingMessage' at every node:http call site (DOD-DOC-LEAF-1, 2026-08-04). This override SILENTLY wins over the ^25.6.2 declarations in packages/interfaces, test-fixtures, operations-agent and demo -- bump this line together with those, or the declared ranges become a lie. Removable once every package agrees on one major."
  },
  "name": "trustless-cello",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "typecheck": "tsc --build",
    "test": "vitest run",
    "test:all": "pnpm -r --workspace-concurrency=1 run test",
    "lint": "eslint packages/*/src",
    "dev": "pnpm --filter @cello-protocol/directory run start & pnpm --filter @cello-protocol/relay run start"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@journeyapps/sqlcipher",
      "esbuild",
      "better-sqlite3",
      "argon2",
      "@oqs/liboqs-js",
      "agentdb",
      "hnswlib-node",
      "cbor-extract",
      "protobufjs",
      "sharp"
    ],
    "overrides": {
      "@libp2p/interface": "3.2.2",
      "@types/node": "25.6.2"
    },
    "patchedDependencies": {
      "@libp2p/autonat@3.0.20": "patches/@libp2p__autonat@3.0.20.patch"
    }
  },
  "devDependencies": {
    "@claude-flow/testing": "3.0.0-alpha.2",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.4.0",
    "vitest": "^3.0.0"
  }
}
