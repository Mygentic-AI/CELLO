{
  "name": "@cello/e2e-tests",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "typecheck": "tsc --build",
    "test": "vitest run",
    "test:cross-machine": "vitest run --config vitest.cross-machine.config.ts"
  },
  "dependencies": {
    "@cello/adapter-claude-code": "workspace:*",
    "@cello/client": "workspace:*",
    "@cello/crypto": "workspace:*",
    "@cello/directory": "workspace:*",
    "@cello/protocol-types": "workspace:*",
    "@cello/relay": "workspace:*",
    "@cello/transport": "workspace:*"
  }
}
